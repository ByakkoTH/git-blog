<h1>Listando Artículos</h1>
 <%= link_to  "create", new_article_path %>
<table>
  <tr>
    <th>Titular</th>
    <th>Contenido</th>
  </tr>

  <% @article.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
    </tr>
  <% end %>
  
</table>